--Ensure the database server and credentials for hte user are correct and have enough privileges. Else web api won't initialize 

--1. Create optional schema
--2. Create tables
--3. Remember to end the script in GO